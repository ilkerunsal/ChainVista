apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-db
spec:
  type: {{ .Values.db.service.type }}
  selector:
    app: {{ .Release.Name }}-db
  ports:
    - port: {{ .Values.db.service.port }}
      targetPort: 5432